# Supabase — projeto da ON ACTION DISTRIBUIDORA
# Preencher depois de criar/linkar o projeto (não commitar o .env real).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Só usada em código server-side (rotas de API, server actions).
# Nunca expor com prefixo NEXT_PUBLIC_ e nunca importar em componente de cliente.
SUPABASE_SERVICE_ROLE_KEY=
